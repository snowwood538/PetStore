package swagger.petstore.wiremock.urls;

import static swagger.petstore.wiremock.urls.Domain.HOST_MOCK;
import static swagger.petstore.wiremock.urls.Domain.PORT_MOCK;

public class BaseUrls {
    public static final String MOCK_URL = "http://" + HOST_MOCK + ":" + PORT_MOCK;
}
